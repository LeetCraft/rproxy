{
  "name": "rproxy",
  "version": "2.1.0",
  "description": "A world-class reverse proxy with automatic HTTPS",
  "type": "module",
  "bin": {
    "rproxy": "./cli.ts"
  },
  "scripts": {
    "build": "bun build cli.ts --compile --outfile rproxy",
    "dev": "bun --hot server.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "latest"
  }
}
